# Data

The notebooks load the original California housing CSV directly from this public URL:

https://raw.githubusercontent.com/ageron/handson-ml/master/datasets/housing/housing.csv

Run `notebooks/02_data_cleaning.ipynb` to create:

```text
data/cleaned_house_data.csv
```

The cleaned CSV is generated locally so the model training notebook can load a simple prepared dataset.

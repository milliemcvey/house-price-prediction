# Streamlit App

This folder contains a small visual front end for the project.

Run it from the project root:

```bash
streamlit run app/streamlit_app.py
```

The app trains the same beginner-friendly linear regression baseline used in the notebooks and lets you change input values with sliders.
